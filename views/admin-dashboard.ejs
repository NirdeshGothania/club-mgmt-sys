<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css">
    <title>Admin Dashboard</title>
</head>
<body>
    <ul>
        <% if(typeof errors != 'undefined'){ %>
            <% errors.forEach(error => { %>
                <li><%= error.message %></li>
            <% }) %> 
        <% } %>
    </ul>

    <h1>Hello <%= user %></h1>
    <h1>Welcome to the Admin Dashboard</h1>
    <p>You have access to admin features.</p>
    
    <div>
    <ul>
        <!-- <% if (students != null) students.forEach(student => { %>
            <li>
                <%= student.name %>
                <form action="/students/dashboard/promote-coordinator/<%= student.user_id %>" method="POST">
                    <label for="clubId">Select Club:</label>
                    <select name="clubId" id="clubId">
                        <% clubs.forEach(club => { %>
                            <option value="<%= club.club_id %>"><%= club.name %></option>
                        <% }); %>
                    </select>
                    <button type="submit">Promote to Coordinator</button>
                </form> -->

                <% if (students != null) students.forEach(student => { %>
                    <li>
                        <%= student.name %>
                        <form action="/students/dashboard/promote-coordinator/<%= student.user_id %>" method="POST">
                            <label for="clubId">Select Club:</label>
                            <select name="clubId" id="clubId">
                                <option value="">Select a Club</option> <!-- Add a default option -->
                                <% clubs.forEach(club => { %>
                                    <option value="<%= club.club_id %>"><%= club.club_name %></option>
                                <% }); %>
                            </select>
                            <button type="submit">Promote to Coordinator</button>
                        </form>
                    </li>
                <% }); %>

                <!-- <form method="POST" action="/students/promote-coordinator/<%= user.user_id %>">
                    <label for="clubId">Select Club:</label>
                    <select name="clubId" id="clubId">
                        <% clubs.forEach(club => { %>
                            <option value="<%= club.club_id %>"><%= club.name %></option>
                        <% }); %>
                    </select>
                    <button type="submit">Promote to Coordinator</button>
                </form> -->

            </li>
        <% }); %>
    </ul>
    </div>

    <!-- <div>
        <h2>Club List</h2>
        <ul>
            <% clubs.forEach(club => { %>
                <li>
                    Club Name: <%= club.name %>
                    <% if (club.user_id) { %>
                        Coordinator: Yes
                    <% } else { %>
                        Coordinator: No
                    <% } %>
                </li>
            <% }); %>
        </ul>
    </div> -->


    <!-- <div>
        <h2>Club List</h2>
        <ul>
            <% clubs.forEach(club => { %>
                <li>
                    Club Name: <%= club.name %>
                    <% if (club.user_id) { %>
                        Coordinator: Yes, Coordinator Name: <%= club.coordinator_name %>
                    <% } else { %>
                        Coordinator: No
                    <% } %>
                </li>
            <% }); %>
        </ul>
    </div> -->


    <!-- <div>
        <h2>Club List</h2>
        <ul>
            <% clubs.forEach(club => { %>
                <li>
                    Club Name: <%= club.club_name %>
                    <% if (club.coordinator_name) { %>
                        Coordinator: <%= club.coordinator_name %>
                    <% } else { %>
                        Coordinator: No Coordinator Assigned
                    <% } %>
                </li>
            <% }); %>
        </ul>
    </div> -->

    <div>
        <h2>Club List</h2>
        <ul>
            <% clubs.forEach(club => { %>
                <li>
                    Club Name: <%= club.club_name %>
                    <% if (club.coordinators.length > 0) { %>
                        Coordinator(s): <%= club.coordinators.join(', ') %>
                    <% } else { %>
                        Coordinator: No Coordinator Assigned
                    <% } %>
                </li>
            <% }); %>
        </ul>
    </div>

    <a href="/students/create-clubs">Create Club</a>
    <br>
    <a href="/students/logout">Logout</a>
</body>
</html>